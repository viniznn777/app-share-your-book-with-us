import styled from "styled-components";
import { fontColor } from "../../../Home/Styles";

const ContainerMyRecommendations = styled.div`
  padding-top: 99px;
  hr {
    color: ${fontColor};
  }
  p.fs-2 {
    color: #fff;
  }
`;
export default ContainerMyRecommendations;
