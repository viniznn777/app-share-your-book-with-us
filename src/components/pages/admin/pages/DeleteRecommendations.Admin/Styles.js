import styled from "styled-components";

const ContainerDeleteRecommendations = styled.div`
  padding-top: 99px;

  hr {
    color: greenyellow;
  }
`;

export default ContainerDeleteRecommendations;
